var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
parcelHelpers.defineInteropFlag(exports);
var _view = require("./view");
var _viewDefault = parcelHelpers.interopDefault(_view);
class OverviewView extends _viewDefault.default {
    parentElement = document.querySelector('.overview');
    message = "It seems that an error has occurred! Please please try again";
    generateMarkup() {
        return this._data.map(this._generateMarkupImages);
    }
    _generateMarkupImages(image) {
        return `\n            div.overview__item\n                a(href="#")\n                    img(src="${image.imageURL}", alt="${image.tags}")\n            h2 ${image.tags}\n        `;
    }
    addHandleLoadOverview() {
    //   _renderHits
    }
}
exports.default = new OverviewView();
